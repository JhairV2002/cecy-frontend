import { FilterFreePipe } from './filter-free.pipe';

describe('FilterFreePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterFreePipe();
    expect(pipe).toBeTruthy();
  });
});

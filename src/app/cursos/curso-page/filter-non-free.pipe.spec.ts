import { FilterNonFreePipe } from './filter-non-free.pipe';

describe('FilterNonFreePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterNonFreePipe();
    expect(pipe).toBeTruthy();
  });
});
